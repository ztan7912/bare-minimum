[project]
name = "BareMinimumQueueTimeAdded"
version = "0.1.0"
description = "Bare minimum UiPath Python function that adds the current UTC time to Test_Queue."
authors = [{ name = "Zheng Tan" }]
requires-python = ">=3.11"
dependencies = [
    "uipath>=2.10.70",
]
